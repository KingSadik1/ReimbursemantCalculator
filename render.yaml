services:
  - type: web
    name: reimbursement-calculator
    env: static  # It's still a static environment, but as a web service
    branch: main
    buildCommand: ""  # No build command since it's a static site
    startCommand: ""  # No start command needed for static files
    staticPublishPath: .  # Specify the folder where your index.html file is located


